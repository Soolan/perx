import { WhereAreMyPerxDirective } from './where-are-my-perx.directive';

describe('WhereAreMyPerxDirective', () => {
  it('should create an instance', () => {
    const directive = new WhereAreMyPerxDirective();
    expect(directive).toBeTruthy();
  });
});
