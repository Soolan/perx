import { PerxDirective } from './perx.directive';

describe('PerxDirective', () => {
  it('should create an instance', () => {
    const directive = new PerxDirective();
    expect(directive).toBeTruthy();
  });
});
